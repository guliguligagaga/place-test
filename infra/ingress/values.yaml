enabled: true
entryPoints:
  - web
routes: []
  # Example route configuration:
  # routes:
  #   - match: Host(`example.com`) && PathPrefix(`/`)
  #     service:
  #       name: frontend
  #       port: 80
  #   - match: Host(`example.com`) && PathPrefix(`/api`)
  #     middlewares:
  #       - auth-middleware
  #     service:
  #       name: backend
  #       port: 8080

# Define middlewares separately if needed
middlewares: []
# Example middleware configuration:
# middlewares:
#   - name: auth-middleware
#     forwardAuth:
#       address: http://auth-service/verify