version: '3.8'

services:
  django:
    build:
      context: ./project
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./project:/app
    depends_on:
      - rust_backend
    environment:
      - DEBUG=1
    networks:
      - my_network

  rust_backend:
    build:
      context: ./untitled
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
        - redis
    volumes:
      - ./untitled:/usr/src/app
    networks:
      - my_network

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - django
      - rust_backend
    networks:
      - my_network
  
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - my_network

networks:
  my_network:
    name: custom_network